package {{ meta.package }}.pojo;

import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
import lombok.ToString;
{% for import in meta.imports -%}
import {{ import }};
{% endfor %}
import com.sictiy.common.db.DataObject;
import com.sictiy.common.entry.annotation.CommomAnnotation;

/**
* {{ meta.comment }}
*
* generated by tool
* @since: {{ meta.get_date() }}
*/
@Setter
@Getter
@ToString
@NoArgsConstructor
@CommomAnnotation(str = "{{ meta.table }}")
public class {{ meta.java_name }}Info extends DataObject
{{ '{' }}{% for field in meta.fields %}
    /**
    * {{ field.comment }}
    */
    {{ field.java_type }} {{ field.name }};
{% endfor %}{{ '}' }}